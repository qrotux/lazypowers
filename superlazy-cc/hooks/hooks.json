{
  "description": "superlazy-build seam gate: deny advancing to writing-plans / execution until the prior critic's marker exists",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Skill",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}\"/hooks/superlazy-build-gate.sh"
          }
        ]
      }
    ]
  }
}
